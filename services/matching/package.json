{
  "name": "@towcommand/services-matching",
  "version": "1.0.0",
  "description": "Provider matching engine for TowCommand",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@towcommand/core": "workspace:*",
    "@towcommand/db": "workspace:*",
    "@towcommand/cache": "workspace:*",
    "@towcommand/events": "workspace:*",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
